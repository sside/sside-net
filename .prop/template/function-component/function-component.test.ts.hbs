import { expect, test } from "next/experimental/testmode/playwright/msw";

test.describe("{{{PascalFunctionComponentName}}}", () => {
test.beforeEach(async ({ page }) => {
await page.goto("/");
});

test("", async ({ page }) => {
const locator = page.locator(".{{{kebabFunctionComponentName}}}")
await expect(locator);
});
});
